---
import { themeConfig } from '@/config'
import IndexLayout from '@/layouts/IndexLayout.astro'
---

<IndexLayout title={`404 - ${themeConfig.site.title}`} description="Pagina no encontrada">
  <style>
    .error-container {
      color: var(--text-secondary);
    }
  </style>
  <p class="error-container">Pagina no encontrada...</p>
</IndexLayout>
